        temp=[0]*len(nums)
        count=0
        for i in range(len(nums)):
            if nums[i]!=0:
                temp[count]=nums[i]
                count+=1
        for i in range(len(nums)):
            nums[i]=temp[i]
